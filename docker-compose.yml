
services:
  docuchat:
    build: .
    ports:
      - "8501:8501"
    environment:
      - USER_AGENT=docuchat_agent
    volumes:
      - ./source_files:/app/source_files
    restart: unless-stopped
